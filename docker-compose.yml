version: "3.9"

services:
  multi-agent-ai:
    build: .
    ports:
      - "8501:8501"
    environment:
      MODEL_NAME: gemma2:2b
      OLLAMA_HOST: http://host.docker.internal:11434
      TEMPERATURE: 0.2